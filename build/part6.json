[
{"id":126,"d":3,"q":"You need deeper LDAP logging on the Vault. Which form does the `DebugLevel` entry in `dbparm.ini` take?","opts":["A single number from 0 to 6","Named modules with levels, e.g. `DebugLevel=LDAP(14,15)` or `DebugLevel=PE(1),PERF(1)`","`DebugLevel=High`","`DebugLevel=Trace`"],"a":[1],"why":"Vault debugging is per-module: you name the module and the levels you want, such as `PE(1),PERF(1)` for the policy engine and performance, or `LDAP(14,15)` for directory troubleshooting. That keeps `Trace.d0`–`Trace.d4` focused instead of drowning you in everything at once.","wrong":"A is the CPM's `CPMDebugLevels` scale (0 none, 1 exceptions, 2 trace, 3–6 CASOS). C is the PVWA's style. D is not a valid value. Remember the change needs a Vault service restart.","ref":"Configuring debug levels"},

{"id":127,"d":3,"q":"Which values do the PVWA's `DebugLevel` and `InformationLevel` settings take?","opts":["0 to 6","None, Low, High, Profiling","1 to 7","Yes / No"],"a":[1],"why":"The PVWA uses named levels — None, Low, High and Profiling. Profiling is the heaviest and is for performance investigations rather than routine troubleshooting; leaving it on in production will fill the log folder quickly.","wrong":"A is the CPM's scale. C is the PSM server trace range (the Recorder and Client sub-levels are 1–2). D is not a debug scale.","ref":"Configuring debug levels"},

{"id":128,"d":3,"q":"A PSM issue needs deeper tracing. Which statement about PSM `TraceLevels` is correct?","opts":["There is a single level from 1 to 7 covering everything","The Server level runs 1–7, while the Recorder and Client levels each run 1–2","It is a Yes/No switch","It is set in the Master Policy"],"a":[1],"why":"PSM tracing is split by subsystem: the PSM server itself has the widest range at 1–7, while the recorder and the client each have just two levels. Raising only the subsystem you suspect keeps the logs readable.","wrong":"A misses the split. C describes the DR service's `EnableTrace=yes`. D is where access policy lives, not diagnostics.","ref":"Configuring debug levels"},

{"id":129,"d":3,"q":"Which two `PADR.ini` parameters determine how often the DR Vault replicates and how quickly it notices the production Vault is gone? (Choose two)","opts":["`ReplicateInterval` — seconds between replications, default 3600","`CheckInterval` — seconds between availability checks, default 60","`EnableDbsync`","`AccessVaultForInactivity`"],"a":[0,1],"why":"`ReplicateInterval` (3600 seconds, i.e. hourly) sets the data replication cadence. `CheckInterval` (60 seconds) is how often the DR service probes the production Vault; combined with `CheckRetriesCount` of 4 and a 30-second gap between retries, that is how long it takes before failover logic engages.","wrong":"C controls database synchronisation and D controls whether the DR service accesses the Vault to test for inactivity — both real parameters, neither of which sets these two intervals. `EnableFailover` and `FailoverMode` govern whether failover happens at all.","ref":"PADR.ini"},

{"id":130,"d":3,"q":"Put the DR failover steps into the correct order.","opts":["Start the PrivateArk Server → synchronise the database → start ENE → stop the DR service","Synchronise the database → start the PrivateArk Server → start ENE → stop the DR service","Stop the DR service → start the PrivateArk Server → synchronise the database → start ENE","Start ENE → synchronise the database → start the PrivateArk Server → stop the DR service"],"a":[1],"why":"Synchronise first so the DR Vault holds the most complete data set available, then bring the Vault service up on that data, then start the notification engine, and only then stop the DR replication service — because while it is running it will keep trying to behave like a replica.","wrong":"The other orders either start the Vault on unsynchronised data or stop the DR service before the Vault is serving, which is how people lose the last replication increment.","ref":"DR failover"},

{"id":131,"d":3,"q":"Production has been rebuilt and you are failing back from the DR Vault. What does the failback procedure require?","opts":["Set `FailoverMode=No`, delete the last two lines of `PADR.ini` to force a full replication, and restart the DR service","Reinstall the DR Vault from scratch","Run `PARestore` against the production Vault","Nothing — failback is automatic once production responds"],"a":[0],"why":"The last two lines of `PADR.ini` hold the replication position. Removing them makes the DR service forget where it was and perform a full replication rather than an incremental one, which is what you want when the production Vault's data has been rebuilt or diverged. `FailoverMode=No` returns the DR to replica behaviour.","wrong":"B is unnecessarily destructive. C restores Safes from backup files, a different operation. D is dangerous — automatic failback risks split brain.","ref":"DR failback"},

{"id":132,"d":3,"q":"Which two conditions must be met before `PAReplicate.exe` can take a Vault backup? (Choose two)","opts":["The built-in `Backup` user must be enabled and given a password","A credential file for that user must exist, referenced with `/logonfromfile`","The Vault service must be stopped for the duration","The `DR` user must be suspended"],"a":[0,1],"why":"The command form is `PAReplicate.exe vault.ini /logonfromfile user.ini /FullBackup`. The `Backup` user ships disabled and without a password, so enabling it and setting one is a genuine setup step people forget, and like every component it authenticates with a credential file.","wrong":"C is false and is the point of the replicator — backups run against a live Vault. D would break disaster recovery. Note that restoring with `PARestore.exe` requires the Restore All Safes authorization.","ref":"PAReplicate / backup"},

{"id":133,"d":3,"q":"Which authorization is required to restore a Safe with `PARestore.exe`?","opts":["Backup All Safes","Restore All Safes","Manage Safe","Audit Users"],"a":[1],"why":"Backup and restore are deliberately separate authorizations. Backup All Safes lets a user copy data out; Restore All Safes lets a user write data back in, which is the far more dangerous capability and is why the `DR` user holds both while the `Backup` user's role is narrower.","wrong":"A permits the backup, not the restore. C is a Safe-level property permission. D is an audit authorization.","ref":"PARestore / Vault authorizations"},

{"id":134,"d":3,"q":"`DRNotificationThreshold = Yes, Yes, 2, 24, 30m`. What does the final value mean?","opts":["Notifications stop after 30 minutes","The DR status is checked every 30 minutes","The first notification is sent after 30 minutes","Replication runs every 30 minutes"],"a":[1],"why":"The fields are: monitor, notify, hours before the first notification, hours between subsequent notifications, and how often to check. The DR threshold is the one where that last value is expressed in **minutes** rather than hours — a deliberate difference from `BackupNotificationThreshold`, whose equivalent field is in hours.","wrong":"A and C misread the position. D is `ReplicateInterval` in `PADR.ini`, a different setting in a different file.","ref":"dbparm.ini notification thresholds"},

{"id":135,"d":3,"q":"Which statement about CPM log handling is correct?","opts":["Logs grow indefinitely and must be truncated manually","Old logs are rotated into `Logs\\Old`, with third-party plug-in logs going to `Logs\\Old\\ThirdParty`","All CPM logs are uploaded to the Vault for retention","Logs are written only to the Windows Event Log"],"a":[1],"why":"The CPM rotates its own logs into `Logs\\Old`, with per-plug-in logs archived under `Logs\\Old\\ThirdParty`. Worth knowing alongside this: third-party plug-in logs are not uploaded to the Vault, so if you need one for a support case you must collect it from the CPM server.","wrong":"A ignores built-in rotation. C over-states what is uploaded. D is wrong — CyberArk components log to files.","ref":"CPM logging"},

{"id":136,"d":3,"q":"After changing a PVWA configuration file on disk, what is the standard step to make IIS pick it up, and how do you confirm the service state?","opts":["`iisreset /restart`, then `iisreset /status`","Restart the PrivateArk Server service","Run `CreateCredFile.exe`","Wait 20 minutes for the automatic refresh"],"a":[0],"why":"The PVWA is an IIS application, so `iisreset /restart` reloads it and `iisreset /status` confirms the services came back. This is also the final step after PKI changes to `applicationHost.config`.","wrong":"B restarts the Vault, which is unnecessary and disruptive. C rebuilds a credential file. D is the PVWA's periodic *configuration* refresh from the Vault — it does not reload files edited on the web server itself.","ref":"PVWA administration"},

{"id":137,"d":3,"q":"Which file and utility belong to a Cluster Vault deployment?","opts":["`PADR.ini` and `PAReplicate.exe`","`ClusterVault.ini` and `StorageManager.exe`","`tsparm.ini` and `CAVaultManager`","`Basic_psm.ini` and `PSMHardening.ps1`"],"a":[1],"why":"`ClusterVault.ini` carries the logical node names, the virtual IP, the peer and local public and private IPs, the `StorageIdentifier` and the `QuorumDiskIdentifier`. `StorageManager.exe` identifies the disks — `-q` for the quorum drive and `-s` for the shared storage drive, as in `StorageManager.exe -qE -sF`.","wrong":"A belongs to DR replication. C is Vale storage location configuration and the Vault management utility. D is the PSM.","ref":"Cluster Vault"},

{"id":138,"d":4,"q":"What is the relationship between the Master Policy and platforms?","opts":["The Master Policy sets baseline access rules for the whole environment, and exceptions can be defined at platform level for populations that need different treatment","Platforms override the Master Policy automatically","The Master Policy applies only to Windows accounts","They are alternative configuration models — you use one or the other"],"a":[0],"why":"The Master Policy is the single place where the organisation's baseline access rules are expressed — dual control, reason for access, exclusive access, one-time passwords, session monitoring and recording. Where one population genuinely needs different treatment, you add an exception scoped to a platform rather than weakening the baseline for everyone.","wrong":"B inverts the model — an exception must be created deliberately, nothing overrides silently. C and D are wrong; the Master Policy is universal and works together with platforms.","ref":"Master Policy"},

{"id":139,"d":4,"q":"Which of these are Master Policy rules rather than platform parameters? (Choose three)","opts":["Require dual control password access approval","Enforce check-in/check-out exclusive access","Enforce one-time password access","`MinValidityPeriod`"],"a":[0,1,2],"why":"The Master Policy expresses *what the organisation requires* of privileged access — approval, exclusivity, one-time use, session monitoring, recording, reason for access. Those are policy statements, so they live centrally.","wrong":"D is a platform parameter that controls *how* the mechanism behaves — the number of minutes the CPM waits after retrieval before replacing a one-time password. Policy says \"one-time\"; the platform parameter says \"after 60 minutes\". Keeping that division clear answers a lot of exam questions.","ref":"Master Policy rules"},

{"id":140,"d":4,"q":"You need dual control for production databases but not for test databases, and both use the same platform. What is the cleanest approach?","opts":["Turn dual control off globally and rely on training","Duplicate the platform so production and test have their own, then apply the exception to the production platform","Move all databases into one Safe","Edit the built-in platform directly"],"a":[1],"why":"Exceptions are scoped to a platform, so two different policies means two platforms. Duplicating the platform is the standard answer to \"same system type, different policy\" — it is the same reason you would split `LIN SSH 30` from `LIN SSH 90`.","wrong":"A abandons the control. C makes the problem worse by merging populations that need different rules. D is not the way to customise, since built-in platforms are not meant to be edited and can be overwritten on upgrade.","ref":"Master Policy exceptions / duplicate a platform"},

{"id":141,"d":4,"q":"`EnforcePasswordVersionsHistory` is set to 7 on a platform. What does that mean?","opts":["Seven password versions are retained in the Safe","The CPM will not reuse any of the last seven generated passwords","Passwords expire after seven days","Seven failed changes trigger an alert"],"a":[1],"why":"This is password *history* enforcement — the CPM avoids regenerating any of the last seven values, mirroring the history rules the target operating system enforces itself. Valid values run 1–50, or -1 to disable.","wrong":"A is Safe version retention, a Safe property (\"save the latest N versions\", default five). C and D are invented.","ref":"Automatic Password Management — Password Change"},

{"id":142,"d":4,"q":"`RotationLatency` is documented as applying only in one circumstance. Which?","opts":["Only when `HeadStartInterval` is 0","Only when the account uses one-time passwords","Only for UNIX platforms","Only during the FromHour/ToHour window"],"a":[0],"why":"`RotationLatency` (default 0, range 0–60 minutes) introduces a small random delay so that a large population of accounts due at the same moment does not all rotate simultaneously. It only applies when `HeadStartInterval` is 0 — because a head start already spreads the work across days.","wrong":"B, C and D are not the documented condition. This pairing is exactly the kind of interaction between two parameters that exam questions like to probe.","ref":"Automatic Password Management — Password Change"},

{"id":143,"d":4,"q":"What do `FromHour` and `ToHour` control on a platform?","opts":["The hours during which users may open the Safe","The window during which the CPM may perform password operations for accounts on that platform","The PSM session duration limit","The hours during which reports may be generated"],"a":[1],"why":"They define the CPM's working window, so credential changes can be confined to a maintenance period rather than landing in the middle of a trading day. `ResetOveridesTimeFrame` (default Yes) lets an immediate reset ignore the window when something urgent is required.","wrong":"A is the Safe's Restrictions tab in the PrivateArk Client — a different control that produces ITATS100E when breached. C and D are unrelated.","ref":"Automatic Password Management"},

{"id":144,"d":4,"q":"What does `ChangeNotificationPeriod` do, and what does -1 mean?","opts":["The number of seconds before a change that a notification is issued in an Application Access Manager environment; -1 means no notification","The number of days before expiry that the CPM starts changing; -1 means immediate","The number of retries after a failed change; -1 means unlimited","The number of minutes between CPM loops; -1 disables the platform"],"a":[0],"why":"It exists for application credentials: applications retrieving a credential through AAM need warning that the value is about to change so they can re-fetch rather than fail. Setting it to -1 turns notification off.","wrong":"B is `HeadStartInterval`, which is in days. C is invented. D is `Interval`.","ref":"Automatic Password Management — Password Change"},

{"id":145,"d":4,"q":"What does the CPM do during a password verification?","opts":["It changes the password and confirms the new one works","It logs on to the target with the password stored in the Vault to confirm the two still match, and flags the account as unsynchronised if they do not","It reads the password hash from the target and compares it","It checks the password against the platform's complexity policy"],"a":[1],"why":"Verification is a read-only health check: prove the stored credential still opens the door. If it does not, the account is flagged unsynchronised — and with `RCAutomaticReconcileWhenUnsynched = Yes` that flag is what triggers reconciliation.","wrong":"A is a change. C is impossible — passwords are stored as one-way hashes on the target. D is a policy check, not a verification.","ref":"Verify passwords"},

{"id":146,"d":4,"q":"An account is marked to reset immediately, but the platform's `MinValidityPeriod` has not yet elapsed and the current time is outside `FromHour`/`ToHour`. What happens with default settings?","opts":["The reset waits for both conditions","The reset proceeds — `ResetOveridesMinValidity` and `ResetOveridesTimeFrame` both default to Yes","The reset is cancelled and must be re-issued","Only the time frame is overridden"],"a":[1],"why":"Both override parameters default to Yes precisely because an immediate reset usually means something has gone wrong — a suspected compromise, a departure, an incident. Making it wait for a maintenance window would defeat the purpose.","wrong":"A and D assume defaults that are not the shipped ones. C is not a behaviour the CPM has.","ref":"Automatic Password Management — Privileged Account Management"},

{"id":147,"d":4,"q":"What is an account group used for?","opts":["Grouping Safe members for permission assignment","Keeping several accounts synchronised on the same password value, for cases where the same credential must exist identically on multiple systems","Grouping platforms for reporting","Grouping accounts to be onboarded together"],"a":[1],"why":"Account groups solve the case where several accounts must genuinely hold the same password — a clustered application, a set of appliances sharing a service identity — so the CPM changes them as a unit and keeps the value identical.","wrong":"A is Vault groups. C and D describe things account groups are often confused with. Note the related but distinct construct: a *group platform* determines when the coordinated change happens, while each account keeps its own target platform for the change mechanics.","ref":"Account groups / group platforms"},

{"id":148,"d":4,"q":"Which Safe permission puts the Change, Verify and Reconcile buttons in front of a user?","opts":["Manage Safe","Initiate CPM account management operations","Retrieve Accounts","Use Accounts"],"a":[1],"why":"That permission is what lets a Safe member trigger CPM work on demand rather than waiting for the scheduled loop. It is separate from being able to see or use the credential, which is the point of granular Safe permissions.","wrong":"A governs Safe properties. C reveals the secret. D allows a PSM session. Note that a user can hold this without holding Retrieve — a genuinely useful separation for an operations team that must fix accounts without reading passwords.","ref":"Safe member permissions"},

{"id":149,"d":4,"q":"What is Object Level Access Control (OLAC), and what should you know before enabling it?","opts":["It restricts a Safe by IP address; it can be toggled freely","It allows permissions to be set on individual accounts within a Safe rather than only at Safe level — and once enabled on a Safe it cannot be disabled","It encrypts individual objects with separate keys; it is enabled by default","It limits the number of objects in a Safe; it is set at platform level"],"a":[1],"why":"OLAC moves the access-control boundary from the Safe down to individual objects inside it. The catch that matters operationally is that it is a one-way switch — a Safe with OLAC enabled cannot have it turned off, so it is a design decision rather than something to experiment with in production.","wrong":"A describes network areas and restrictions. C confuses it with the encryption hierarchy, where every object already has its own File Key. D is `MaxSafeSize` and object limits.","ref":"Safe properties — OLAC"},

{"id":150,"d":4,"q":"A contractor needs access to a Safe for a fixed six-week engagement. What is the cleanest control?","opts":["Set a membership expiration date on their Safe membership","Set `FromHour`/`ToHour` on the platform","Create a Master Policy exception","Set the Safe's object retention to 42 days"],"a":[0],"why":"Safe membership carries an expiration date, which is the built-in way to make temporary access actually temporary rather than depending on someone remembering to remove it. Better still, apply it to a group membership so the pattern is repeatable.","wrong":"B restricts when the CPM operates. C changes access workflow, not duration. D governs how long objects are kept before deletion — nothing to do with who may reach them.","ref":"Safe members — membership expiration"},

{"id":151,"d":4,"q":"A CPM password change for a domain service account succeeds in Active Directory, but updating one of its dependent Windows Services fails. What is the practical consequence?","opts":["Nothing — the service keeps running with the old password","The service will fail to start next time it restarts, because the credential it holds no longer matches the domain","The domain password is rolled back automatically","The account is deleted from the Safe"],"a":[1],"why":"A running service holds its token already, so nothing appears broken until it restarts — which is what makes this failure mode dangerous: it surfaces at the next reboot or patch window, long after the change. This is why dependency handling and `SearchForUsages` matter, and why the CPM logs dependency failures separately.","wrong":"A is true only until the next restart, which is precisely the trap. C — there is no automatic rollback. D does not happen.","ref":"Manage dependent accounts"},

{"id":152,"d":4,"q":"What is a rotational group used for?","opts":["Rotating the CPM that manages a Safe","Rotating through a set of accounts that serve the same purpose, so a freshly rotated credential is always available for the next user","Rotating recording Safes on a schedule","Rotating platform assignments between environments"],"a":[1],"why":"Rotational groups exist for high-demand shared access: rather than one account being checked out, changed and unavailable, a pool of equivalent accounts is rotated so someone always has a ready credential. It is the fourth platform tab, alongside Targets, Dependents and Groups.","wrong":"A is Safe-level CPM assignment. C and D are invented. Do not confuse it with a *group* platform, which changes several accounts together rather than cycling between them.","ref":"Manage platforms — rotational groups"},

{"id":153,"d":4,"q":"When you duplicate a platform, what is copied?","opts":["The platform's settings only — accounts stay on the original platform until you move them","The settings and every account associated with the original","The settings and the Safe","Nothing — a duplicate starts from defaults"],"a":[0],"why":"Duplication copies configuration, not data. Existing accounts remain associated with the original platform, and moving them is a separate deliberate action — which is what makes duplication safe to do in production while you prepare a new policy.","wrong":"B would silently change the policy applied to live accounts. C — Safes are unrelated to platforms. D would defeat the purpose of duplicating.","ref":"Duplicate a platform"},

{"id":154,"d":4,"q":"Which statement about the CPM plug-in architecture is correct?","opts":["Plug-ins run on the Vault","Each managed platform type uses a plug-in on the CPM that knows how to verify, change and reconcile on that target, and it writes its own log under `Logs\\ThirdParty\\`","Plug-ins are stored in the `System` Safe","There is one universal plug-in for all platforms"],"a":[1],"why":"The CPM is a scheduler and a policy engine; the platform-specific knowledge of *how* to change a password on Oracle, or a Cisco appliance, or a Windows local account, lives in the plug-in. That separation is why the Marketplace can supply support for new target types without a CPM upgrade.","wrong":"A — the Vault stores and protects, it does not connect to targets. C — platform definitions live in `PasswordManagerShared`; plug-ins are files on the CPM. D would make the Marketplace pointless.","ref":"CPM plug-ins"},

{"id":155,"d":4,"q":"An SSH key and a password for the same UNIX account are both to be managed. What must be true?","opts":["They can share a Safe, but they need separate platforms","They must be in separate Safes and can share a platform","Both must use the `Unix via SSH` platform","SSH keys cannot be managed alongside passwords"],"a":[0],"why":"The Safe is an access-control boundary and can hold both; the platform is the management policy, and password rotation and SSH key rotation are different mechanics, so each needs its own. Remember also that an imported key should be rotated immediately, because the act of entering it exposed it.","wrong":"B reverses which one must be separate. C names the password platform only. D is false — SSH key management is a core capability.","ref":"SSH key management"},

{"id":156,"d":4,"q":"Which of these is the strongest reason to grant Safe permissions to groups rather than to individual users?","opts":["Groups process faster in the CPM","Individually-assigned permissions are lost if the user object is deleted and recreated — which happens routinely with LDAP-provisioned users","The Vault has a limit on individual permissions","Groups are required for reporting"],"a":[1],"why":"Transparent (LDAP) users are provisioned on demand and can be deleted and recreated by ordinary lifecycle events. Permissions attached to the user object vanish with it; permissions derived from group membership come straight back. It also makes access reviews tractable — you review a handful of groups instead of thousands of individual grants.","wrong":"A is not a real performance consideration. C and D are invented.","ref":"Safe members / LDAP transparent users"},

{"id":157,"d":4,"q":"A platform's `AllowedSafes` is left at its default. What is that default and what does it mean?","opts":["Empty — the platform cannot be used until Safes are named","`.*` — the platform can be used in any Safe, and the CPM searches all of them for accounts using it","`^PasswordManager` — only CPM Safes","`*` — all Safes, but only for Windows platforms"],"a":[1],"why":"The default `.*` matches every Safe name. That is fine in a small deployment, but in a large one it means the CPM scans every Safe on every loop for every platform, which is a real performance cost — narrowing `AllowedSafes` is a standard CPM tuning step.","wrong":"A would break the platform out of the box. C and D are invented values.","ref":"Automatic Password Management — General"},

{"id":158,"d":4,"q":"Approximately how many managed passwords can a single optimised CPM support, and what is one of the main levers for reaching that?","opts":["10,000; adding more Safes","100,000; tuning `AllowedSafes`, `Interval`, `MaxConcurrentConnections` and staggering `FromHour`/`ToHour` across platforms","1,000,000; increasing RAM","50,000; enabling OLAC"],"a":[1],"why":"Around 100,000 is the figure to remember for a well-tuned CPM. Reaching it is about not wasting work: scoping platforms to the Safes that matter, spreading the load across the day rather than piling every change into the same hour, and controlling concurrency to each target.","wrong":"A and D understate it and name irrelevant levers. C overstates it dramatically. OLAC is an access-control feature with no bearing on CPM throughput.","ref":"CPM performance tuning"},

{"id":159,"d":4,"q":"Why is \"require users to specify a reason for access\" more than a paperwork exercise?","opts":["It slows users down, which reduces credential use","It attaches intent to each access event in the audit trail, so a later review can distinguish routine work from anomalous access — and it pairs with ticketing integration to tie access to an approved change","It prevents credential theft outright","It is required for the PSM to record"],"a":[1],"why":"Audit records show that access happened; the reason field records why the person said they needed it. That is what makes review possible at scale, and with a ticketing system integrated the reason can be validated against a real change record rather than free text.","wrong":"A is not the intent. C over-claims — it is a detective and deterrent control, not a preventive one. D is false; recording is governed by its own Master Policy rule.","ref":"Master Policy — reason for access"},

{"id":160,"d":4,"q":"Which statement about the Safe as a design unit is most accurate?","opts":["Safes should be as large as possible to reduce administration","A Safe is the unit of access control, so its boundaries should follow who needs access — grouping accounts that share the same audience and the same owners","Each account should have its own Safe for maximum granularity","Safe design should follow the platform, one Safe per platform"],"a":[1],"why":"Everything about a Safe — membership, permissions, the CPM assignment, retention, the audit scope of a report — applies to the whole Safe. So the right boundary is the population of people who should see the same things. Get that wrong and you spend the rest of the deployment fighting it, because the Safe model is the hardest thing to change later.","wrong":"A collapses access control. C is unmanageable at any real scale, and OLAC exists precisely so you do not have to do this. D conflates policy with access — one platform is often used across many Safes.","ref":"Safe model design"},

{"id":161,"d":4,"q":"An operations team needs to run Change and Verify on accounts but must never see the passwords. Which permission set delivers that?","opts":["List Accounts + Initiate CPM account management operations, without Retrieve Accounts","List Accounts + Retrieve Accounts","Manage Safe","Use Accounts + Retrieve Accounts"],"a":[0],"why":"The permissions are deliberately granular so this exact separation is possible: the team can see the accounts and trigger CPM operations on them, but the secret itself is never displayed to them. It is the same principle as the Use-versus-Retrieve split for session access.","wrong":"B gives them the passwords. C gives Safe property administration and still not the operations buttons. D gives both a session and the credential.","ref":"Safe member permissions"},

{"id":162,"d":4,"q":"What is the practical benefit of deactivating platforms you do not use?","opts":["It reduces licence consumption","Better administration — deactivated platforms are hidden from users choosing a platform — and better performance, since the CPM does not process them","It frees space in the Vault","It is required before a platform can be deleted"],"a":[1],"why":"Two benefits, and the exam wants both. Fewer platforms in the picker means fewer wrong choices at onboarding time; and every active platform is work the CPM schedules on every loop, so deactivating the unused ones is genuine tuning.","wrong":"A — licensing counts managed accounts. C — platform definitions are tiny. D is invented. Note that deactivating a target platform also deactivates its dependent platforms.","ref":"Manage platforms"}
]
