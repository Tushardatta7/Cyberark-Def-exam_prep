[
{"id":231,"d":4,"q":"You are creating a shared Safe for the help desk. Which naming constraint is correct?","opts":["The name may not exceed 20 characters","The characters \\ . \" : < > | are not allowed in a Safe name","Safe owners should choose the name so it is memorable to them","Environments, owners and platforms should be combined to minimise the number of Safes"],"a":[1],"why":"Safe names cannot contain the characters `\\ . \" : < > |`. Alongside that, the name is capped at 28 characters and the Safe is the unit of access control, so a short consistent convention such as `HD-WIN-PRD` works and free text does not.","wrong":"A gets the limit wrong — it is 28, not 20. C hands naming to individuals and produces an unmanageable estate. D is the opposite of good design: merging populations that need different access into one Safe destroys the access boundary.","ref":"Safe naming conventions"},

{"id":232,"d":2,"q":"You are configuring the Vault to send syslog audit data to a SIEM. Which is a valid value for the `SyslogServerProtocol` parameter in `dbparm.ini`?","opts":["TLS","SSH","SMTP","SNMP"],"a":[0],"why":"`SyslogServerProtocol` accepts the transports syslog itself supports — TLS, TCP and UDP — with TLS being the one to choose for an audit feed, since it encrypts records in transit. The default port is 514, and like every `dbparm.ini` change it needs a Vault restart.","wrong":"B is a remote shell protocol. C is email — that is the ENE's channel. D is SNMP, configured separately in `paragent.ini` with `SNMPHostIP`, `SNMPTrapPort` and `SNMPCommunity`.","ref":"SIEM / syslog integration"},

{"id":233,"d":7,"q":"What must be done before CyberArk can be configured to use RADIUS authentication?","opts":["Define the CyberArk Vault as a client (agent) on the RADIUS server","Set the authentication method to RADIUS on each user in the PrivateArk Client","Run `CAVaultManager SecureSecretFiles` in the Vault installation folder","Set `RadiusServersInfo` in `dbparm.ini`"],"a":[0],"why":"RADIUS is a mutual arrangement: the RADIUS server only answers requests from clients it knows, so the Vault must first be registered there as a RADIUS client or agent with a shared secret. Everything on the CyberArk side comes afterwards.","wrong":"B and D are both real later steps — you do set `RadiusServersInfo` in `dbparm.ini` and you do set each user's authentication method — but neither works until the RADIUS server accepts the Vault. C is a Vault secrets utility unrelated to RADIUS.","ref":"RADIUS authentication"},

{"id":234,"d":4,"q":"You are onboarding 5,000 UNIX root accounts. The CPM cannot log in directly as root and must use a secondary account. What is the least-privilege way to configure this?","opts":["Configure each CPM to use the correct logon account","Configure each CPM to use the correct reconcile account","Configure the UNIX platform to use the correct logon account","Configure the UNIX platform to use the correct reconcile account"],"a":[2],"why":"Two decisions here. First, the problem is *reaching* the account, not that the password is unknown — so it is a **logon** account, which can be low-privilege and simply elevates. Second, the linkage belongs on the **platform**, so one configuration covers all 5,000 accounts instead of being repeated per CPM.","wrong":"A and B put the setting in the wrong place — a CPM is a worker, not a policy container. D reaches for the reconcile account, which is far more privileged than this situation needs; save it for when the password is unknown or out of sync.","ref":"Linked accounts — logon accounts"},

{"id":235,"d":4,"q":"Your organisation requires that all passwords be rotated every 90 days. Where do you express that requirement?","opts":["The Master Policy","Safe templates","`PVWAConfig.xml`","Individually on each platform"],"a":[0],"why":"A regulatory requirement that applies to everything is exactly what the Master Policy is for — it is the single place the organisation's baseline is stated, so there is one thing to point an auditor at. Platforms then handle the mechanics, and genuine exceptions are added deliberately and visibly.","wrong":"B governs default Safe properties. C is a PVWA configuration file. D would scatter the same rule across dozens of platforms with no single source of truth — the classic mistake this question is testing.","ref":"Master Policy"},

{"id":236,"d":5,"q":"Which built-in PVWA report shows the number of days remaining until a password is due to expire?","opts":["Privileged Accounts Inventory","Privileged Accounts Compliance Status","Activity Log","Applications Inventory"],"a":[1],"why":"Compliance Status is the forward-looking report: it shows whether each account is being managed in line with its policy, including how long is left before the next required change. That makes it the one to run when you want to see what is about to fall out of compliance rather than what already has.","wrong":"A is a census of what exists. C is a record of what has already happened. D covers applications using Application Access Manager.","ref":"PVWA reports"},

{"id":237,"d":3,"q":"Match the component to where its logs live. Which set is correct?","opts":["PTA: `/opt/tomcat/logs` · PSM for SSH: `/var/opt/CARKpsmp/logs` · DR: `…\\PrivateArk\\Server\\PADR`","PTA: `/var/opt/CARKpta` · PSM for SSH: `/opt/tomcat/logs` · DR: `…\\PrivateArk\\Server\\Logs`","PTA: `/opt/PTA/logs` · PSM for SSH: `/opt/psmp/logs` · DR: `…\\PrivateArk\\Safes`","All three write to the Vault's `Logs` folder"],"a":[0],"why":"PTA runs on Tomcat, so its logs sit under `/opt/tomcat/logs`. PSM for SSH keeps its own set under `/var/opt/CARKpsmp/logs` (`PSMPConsole.log`, `PSMPTrace.log`). The DR service logs to the `PADR` folder under the PrivateArk Server directory, where `PADR.log` lives.","wrong":"The other rows shuffle the paths. A useful hook: PTA is a Tomcat application, PSMP is a CARK* package under `/var/opt`, and DR belongs to the PrivateArk Server installation.","ref":"Component log locations"},

{"id":238,"d":3,"q":"Email alert settings on PTA need changing after installation. Which script do you run?","opts":["`/opt/tomcat/utility/emailConfiguration.sh`","`/opt/PTA/emailConfiguration.sh`","`/opt/PTA/utility/emailConfig.sh`","`/opt/tomcat/utility/emailSetup.sh`"],"a":[1],"why":"PTA's post-install email configuration is handled by `emailConfiguration.sh` under `/opt/PTA`. It is worth pairing in your memory with the other PTA utility you may need — `vaultPermissionsValidation.sh` in the utility folder, for re-syncing the `PTA_PAS_Gateway` account.","wrong":"The other options mix up the directory (`/opt/tomcat` is where PTA's logs live, not its utilities) or the script name. Version 14.0 also moved many PTA settings into the PVWA interface, so check there first on current versions.","ref":"PTA — post-installation configuration"},

{"id":239,"d":3,"q":"A network glitch caused the PrivateArk Server to become active on the DR Vault while the Primary Vault was still running normally, and all components stayed pointed at the Primary. What restores DR replication?","opts":["Replicate data from the DR Vault to the Primary, then shut down the PrivateArk Server on DR, then start replication","Shut down the PrivateArk Server on the DR Vault, then start replication on the DR Vault","Shut down the PrivateArk Server on the Primary, replicate from DR to Primary, shut down DR, start replication","Shut down the PrivateArk Server on DR, replicate DR to Primary, shut down DR again, start replication"],"a":[1],"why":"The key detail is that the components never left the Primary — so the Primary holds the authoritative data and nothing on the DR needs preserving. You simply put the DR back into replica mode: stop its PrivateArk Server (a replica must not serve) and restart replication, which pulls a fresh copy from the Primary.","wrong":"A, C and D all replicate *from* the DR to the Primary, which would push stale or divergent data over the good copy. Recognising which side is authoritative is the whole question.","ref":"DR Vault — recovering from split brain"},

{"id":240,"d":2,"q":"Which parameters can be used to harden a credential file when it is generated with `CreateCredFile`? (Choose three)","opts":["The operating system username the file may be used by","The host IP address","The client hostname","The operating system type (Linux / Windows / HP-UX)"],"a":[0,1,2],"why":"Hardening a credential file means binding it to the context it is allowed to be used from — the OS user, the machine's IP address and its hostname, plus an entropy file. Present it from anywhere else and the Vault rejects it, which is why copying a credential file to another server never works.","wrong":"D is not one of the hardening parameters. Nor are the Vault's own IP address or a time frame — all plausible-sounding, none of them real.","ref":"CreateCredFile utility"},

{"id":241,"d":3,"q":"Following a Vault installation, which service must be set to Automatic (delayed start)?","opts":["Windows Time service","PrivateArk Database","Windows Update service","PrivateArk Server"],"a":[0],"why":"The Windows Time service is set to Automatic (delayed start) so time synchronisation is running but does not compete with the Vault services during boot. Accurate time matters more than it looks: it underpins Kerberos, certificate validity and the correlation of audit records across components.","wrong":"B and D are Vault services set to start normally. C should be tightly controlled on a hardened Vault — a Vault is patched deliberately, not by automatic Windows Update.","ref":"Following Vault installation"},

{"id":242,"d":3,"q":"Users cannot launch web-type connection components from the PSM server and Support has asked for logs. Which three help? (Choose three)","opts":["`PSMConsole.log`","`PSMTrace.log`","`<Session_ID>.Component.log` under `PSM\\Logs\\Components`","`ITALog.log`"],"a":[0,1,2],"why":"Diagnosis works from general to specific: `PSMConsole.log` shows what the PSM service did, `PSMTrace.log` adds detail once tracing is raised, and the per-session component log under `PSM\\Logs\\Components` captures what the launched client itself did — which is where a failing web connector actually shows up.","wrong":"D is the Vault's main log and knows nothing about a browser failing to start on a PSM. `PSMDebug.log` and `PMconsole.log` are also distractors — the latter belongs to the CPM.","ref":"PSM log files"},

{"id":243,"d":3,"q":"What is the default username for the PSM for SSH maintenance user?","opts":["`proxymng`","`psmp_maintenance`","`psmpmaintenanceuser`","`psmpmnguser`"],"a":[0],"why":"`proxymng` is the default maintenance account on a PSM for SSH server — the identity you use to administer the host itself rather than being intercepted and brokered like an ordinary SSH connection. Which accounts get that treatment is controlled by `PSMP_MaintenanceUsers` in `sshd_config`.","wrong":"The other three are invented names that follow the pattern you would expect, which is exactly why this is asked as a recall question.","ref":"PSM for SSH — maintenance users"},

{"id":244,"d":6,"q":"A customer wants to keep PSM recordings for 100 days and expects 10 Windows sessions per day of 100 minutes each. Roughly how much storage should you plan for the Vault?","opts":["About 40 GB","About 250 GB","About 500 GB","About 5 GB"],"a":[0],"why":"Work it through: 100 days × 10 sessions × 100 minutes = 100,000 minutes. At 200 KB per minute for low-activity RDP that is 20,000,000 KB ≈ 19 GB; at 300 KB per minute for high-activity RDP it is about 29 GB. Add the 20 GB constant the sizing formula includes and you land near 39–49 GB.","wrong":"B and C over-estimate by roughly an order of magnitude — and note that some third-party question banks give 250 GB here, which the arithmetic does not support. D forgets the 20 GB constant. Remember the rates: 100 / 200 / 300 KB per minute for SSH / low-activity RDP / high-activity RDP.","ref":"PSM recording sizing"},

{"id":245,"d":2,"q":"What is mandatory for a PVWA installation?","opts":["A DNS entry for the PVWA URL","A company-signed TLS certificate imported into the server","A Vault administrative user, used to register the PVWA with the Vault","Data Execution Prevention must be disabled"],"a":[2],"why":"The PVWA has to introduce itself to the Vault: registration creates its component users and its Safes (`PVWAConfig`, `PVWAReports`, `PVWAUserPrefs` and the rest), and that requires an administrative Vault user. Without it the installation cannot complete.","wrong":"A and B are strongly recommended in production but are not installation blockers. D is a security feature you would never disable on a CyberArk component.","ref":"PVWA installation requirements"},

{"id":246,"d":6,"q":"User `neil` needs to reach Linux target 192.168.1.164 as the **domain** account `linuxuser01` on domain `acme.corp`, through the PSM for SSH server 192.168.65.145. What is the correct syntax?","opts":["`ssh neil@linuxuser01:acme.corp@192.168.1.164@192.168.65.145`","`ssh neil@linuxuser01#acme.corp@192.168.1.164@192.168.65.145`","`ssh neil@linuxuser01@192.168.1.164@192.168.65.145`","`ssh neil@linuxuser01@acme.corp@192.168.1.164@192.168.65.145`"],"a":[1],"why":"The base pattern is `vaultuser@targetaccount@targetaddress@psmpaddress`. When the target account is a **domain** account, the domain is attached to the account with a **hash**: `targetaccount#domainaddress`. So the four elements stay in the same order and only the second one gains the `#domain` suffix.","wrong":"A uses a colon. C omits the domain entirely, which would look for a local account. D separates the domain with `@`, which breaks the four-part structure by adding a fifth element.","ref":"PSM for SSH — connection syntax"},

{"id":247,"d":7,"q":"A company requires challenge/response multi-factor authentication for PSM for SSH sessions. Which server must be integrated with the Vault?","opts":["LDAP","PKI","SAML","RADIUS"],"a":[3],"why":"RADIUS is the protocol built around a challenge/response exchange, which is what lets a token, push or one-time code be demanded as a second factor. That is why it is the integration behind MFA for PSMP sessions.","wrong":"A authenticates and provisions users but is single-factor on its own. B is certificate-based. C is a web browser federation protocol and does not apply to an SSH session. Note the related fact: PSM for SSH supports CyberArk password, LDAP and RADIUS authentication.","ref":"RADIUS authentication / PSM for SSH"},

{"id":248,"d":6,"q":"Which browser is supported for PSM web connectors built with the CyberArk Plugin Generator Utility (PGU)?","opts":["Internet Explorer","Google Chrome","Microsoft Edge","Firefox"],"a":[1],"why":"PGU-generated web connectors drive Chrome, which is why PSM hardening includes a Chrome-specific step (`PSMChromeHardening`) and why the hardening variable `$SUPPORT_WEB_APPLICATIONS` exists — a browser inside a PSM session needs some of the lockdown relaxed in a controlled way.","wrong":"A is legacy and long out of support for this purpose. C and D are not the supported target for PGU connectors.","ref":"PSM — Plugin Generator Utility"},

{"id":249,"d":2,"q":"Which Vault authorizations does the CyberArk user performing a CPM installation need?","opts":["Add Safes, Add/Update Users, Manage Directory Mapping","Add Safes, Add/Update Users, Reset Users' Passwords, Activate Users, Manage Server File Categories","Manage Directory Mapping, Backup All Safes, Restore All Safes","Audit Users, Activate Users, Add Network Areas, Manage Directory Mapping"],"a":[1],"why":"Reason from what the installer actually does: it creates the CPM's Safes (Add Safes), creates and configures its component users (Add/Update Users, Reset Users' Passwords, Activate Users) and registers the file categories the CPM needs (Manage Server File Categories).","wrong":"A is incomplete and includes directory mapping, which the CPM install does not touch. C is a backup and recovery set. D is an audit and directory set. None of them can create the component users the CPM needs.","ref":"CPM installation requirements"},

{"id":250,"d":7,"q":"When configuring SAML authentication for the PVWA, which value must be identical on both sides?","opts":["The IdP's EntityID and the `PartnerIdentityProvider Name` in `saml.config`","The IdP's user name and `SingleSignOnServiceUrl`","The IdP's Audience and the `ServiceProviderName` in `saml.config`","The IdP's hash algorithm and the certificate"],"a":[2],"why":"`ServiceProviderName` is the issuer string that identifies the **PVWA** to the identity provider, and it must match the Audience the IdP is configured to expect. A mismatch here is the classic \"SAML response rejected\" cause.","wrong":"A describes a real pairing but in the wrong direction: `PartnerIdentityProvider Name` is where you enter the *IdP's* EntityID so the PVWA can recognise it — that is the mirror of the correct answer, not the same thing. B and D combine unrelated settings. `saml.config` is created by copying `saml.config.template` in the PasswordVault installation folder.","ref":"SAML authentication — saml.config"},

{"id":251,"d":1,"q":"You are using the AccountUploader utility to create accounts with SSH keys. Which parameter gives the path to the private key file to attach?","opts":["`KeyPath`","`KeyFile`","`ObjectName`","`Address`"],"a":[1],"why":"`KeyFile` takes the full or relative path of the SSH private key file that will be attached to the account being created. The utility is the scripted equivalent of onboarding a key by hand, and the same rule applies afterwards: rotate the key immediately, because entering it exposed it.","wrong":"A is a plausible-sounding name that does not exist. C names the object in the Safe and D is the target address — both real properties, neither of them the key file.","ref":"AccountUploader utility"},

{"id":252,"d":2,"q":"What is the recommended way for a load balancer to decide that a PVWA is unavailable and should be removed from the pool?","opts":["Monitor port 443 on the PVWA server","Monitor port 1858 on the PVWA server","Ping the PVWA server","Monitor port 3389 on the PVWA server"],"a":[0],"why":"Health-check the port that actually carries the service. Users reach the PVWA over HTTPS on 443, so a check against 443 tests the thing that matters — the web application responding — rather than something adjacent to it.","wrong":"B is the PVWA's outbound connection to the Vault; it can be perfectly healthy while IIS is down. C only proves the operating system is alive, which is the classic false-positive health check. D is RDP and has nothing to do with the service.","ref":"PVWA load balancing"}
]
